{
  "name": "self-learning-reviewer",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "link-env": "bun scripts/link-env.mjs",
    "db:init": "bun scripts/init-db.mjs",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:generate": "bun --filter @reviewer/db db:generate",
    "db:push": "bun scripts/init-db.mjs",
    "db:studio": "bun --filter @reviewer/db db:studio"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "dotenv-cli": "^8.0.0",
    "prettier": "^3.6.2",
    "turbo": "^2.6.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
